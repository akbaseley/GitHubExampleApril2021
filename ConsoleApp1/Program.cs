using System;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {
            double totalHours; //declaring total hours

            //prompt/input
            Console.WriteLine("Hello C#! How long is class (in hours)?");
            string input = Console.ReadLine();
            //double.Parse is a method that takes in a string and turns it into a double
            //that is stored in our variable called hours
            double hours = double.Parse(input);

            //Manipulate
            totalHours = 0;//mathymaths;

            //Response


            int number = 0;

            //this is the change I'm making for the GitHub class example
        }
    }
}
